{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "schedule": [
    "after 3am and before 4am every weekday"
  ],
  "automerge": false,
  "packageRules": [
    {
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "automerge": false
    },
    {
      "matchPackageNames": ["eslint"],
      "description": "ESLint v10 is too new - most plugins (react, typescript-eslint) don't support it yet. ESLint v9 is the current stable version with full plugin ecosystem support.",
      "allowedVersions": "< 10"
    }
  ]
}
